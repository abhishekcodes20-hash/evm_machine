# evm_machine
A Python-based Electronic Voting Machine (EVM) simulation that verifies voter eligibility, displays political parties dynamically, validates user input, and allows users to cast their vote through an interactive menu-driven system with automatic retry for invalid inputs.
